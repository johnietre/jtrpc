[package]
name = "jtrpc"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
anyhow = "1.0.75"
cfg-if = "1.0.0"
tokio = { version = "1.33.0", features = ["io-util", "net", "rt", "sync", "time"] }
